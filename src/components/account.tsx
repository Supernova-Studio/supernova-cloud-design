// Imports
import * as React from 'react'

type AccountProps = {}

export class Account extends React.PureComponent<AccountProps, {}> {
    // Render the app
    render() {
        return <div />
    }
}
