// Imports
import * as React from 'react'

type DevicesProps = {}

export class Devices extends React.PureComponent<DevicesProps, {}> {
    // Render the app
    render() {
        return <div />
    }
}
